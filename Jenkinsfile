pipeline {
    agent kubernetes{}

    stages {
        stage('Pull Code') {
            steps {
                sh 'ls'
            }
        }
        stage('Deploy') {
            
        }
    }
}
